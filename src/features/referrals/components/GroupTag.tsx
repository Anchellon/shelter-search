// Re-exported from shared — use the shared component directly where possible
export { default, TAG_VARIANTS } from "@/shared/components/GroupTag";
export type { TagVariant } from "@/shared/components/GroupTag";
